# cw_form_designer_library 更新日志

## 1.0.0

这是初始版本，包含上述组件和逻辑，基于当前代码状态生成，无历史变更记录。

- 包含表单设计器（cw-form-designer），支持布局设计与结构导出。
- 包含 JSON 渲染器（cw-form-json-render），支持 JSON 驱动的动态表单生成。
- 包含模版渲染器（cw-form-template-render），支持模版协议及多模式切换。