# cw_countup 更新日志

## 1.0.1

### 变更类型
- 逻辑变更 (logic_change)

### 变更说明
- 更新了依赖库的版本号从 1.0.0 升级到 1.0.1
- 更新了构建产物的路径引用，确保正确引用新版本的资源文件
- 本次变更主要涉及构建配置和版本管理，不影响组件的功能和使用方式

### 受影响文件
- workspaces/legacy-yaml/packages/cw/cw_countup/manifest

### 详细变更
- 将 manifest 文件中的版本引用从 1.0.0 更新为 1.0.1
- 更新了构建产物（demo.html、index.js、index.js.map）的路径引用
- 确保依赖库能够正确加载和使用新版本的资源文件

## 1.0.0

### 初始版本
- 首次发布 cw_countup 依赖库
- 包含 cx-countup 数字渐变组件
- 支持数字从起始值到结束值的平滑动画效果
- 提供可配置的动画参数：起始值、结束值、持续时间、循环播放、延迟间隔和数字分隔符功能