- name: i-status
  title: 状态图标
  description: 请在这里添加描述
  attrs:
    - name: name
      type: string
      description: 图标名称
    - name: size
      type: enum
      options: [small, normal, large]
      default: normal
      description: 图标尺寸
  slots:
    - name: default
      description: 插入文本或 HTML。
