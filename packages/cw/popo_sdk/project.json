{
  "name": "cw-popo_sdk",
  "sourceRoot": "packages/cw/popo_sdk/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm -C packages/cw/popo_sdk run build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm -C packages/cw/popo_sdk run lint || echo 'no lint script'"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm -C packages/cw/popo_sdk run test || echo 'no test script'"
      }
    }
  },
  "tags": [
    "stack:vue2"
  ]
}
