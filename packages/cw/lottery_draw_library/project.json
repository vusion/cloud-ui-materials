{
  "name": "cw-lottery_draw_library",
  "sourceRoot": "packages/cw/lottery_draw_library/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm -C packages/cw/lottery_draw_library run build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm -C packages/cw/lottery_draw_library run lint || echo 'no lint script'"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm -C packages/cw/lottery_draw_library run test || echo 'no test script'"
      }
    }
  },
  "tags": [
    "stack:vue2"
  ]
}
