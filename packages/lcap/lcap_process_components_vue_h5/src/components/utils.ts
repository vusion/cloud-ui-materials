// 后端官方库名称(以依赖库的名称为唯一标识，应用必须包含流程系统逻辑官方依赖库)
export const backendOfficialLibName = 'lcap_process_framework';

// 后端官方库逻辑的命名空间
export const logicNamespace = `extensions.${backendOfficialLibName}.logics`;

// 后端官方库结构体的命名空间
export const structureNamespace = `extensions.${backendOfficialLibName}.structures`;
